# No VITE_ env variables are required at the moment.
# The Simli face_id and api_key are owned by the backend (settings.simli_face_id /
# settings.simli_api_key); the frontend obtains a session_token + ICE servers via
# POST /simli/token, so no Simli credentials live in the browser.
